# 🚂 Variables de Entorno para Railway PostgreSQL
# ================================================
# Copia este archivo a .env y reemplaza los valores con los de tu proyecto Railway

# Opción 1: Usar DATABASE_URL (recomendado)
# Copia esta variable directamente desde Railway
DATABASE_URL=postgresql://postgres:zCjaRumchbAxNBXxooqAbyStfYNDVnod@mainline.proxy.rlwy.net:58698/railway

# Opción 2: Variables individuales (Railway las genera automáticamente)
PGHOST=postgres.railway.internal
PGPORT=5432
PGDATABASE=railway
PGUSER=postgres
PGPASSWORD=zCjaRumchbAxNBXxooqAbyStfYNDVnod

# Variables alternativas (para compatibilidad con código legacy)
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_NAME=${PGDATABASE}
DB_USER=${PGUSER}
DB_PASSWORD=${PGPASSWORD}

# ⚠️ IMPORTANTE:
# 1. NO subas este archivo .env al repositorio (ya está en .gitignore)
# 2. Obtén los valores reales desde Railway > Tu Proyecto > Variables
# 3. DATABASE_URL tiene prioridad sobre las variables individuales
# 4. Para conexión local, usa DATABASE_PUBLIC_URL que permite acceso externo
